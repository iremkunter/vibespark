{
  "build": {
    "web": {
      "commands": ["npm run build"],
      "outputDir": "dist"
    }
  },
  "environments": {
    "production": {
      "build": {
        "web": {
          "commands": ["npm run build"]
        }
      }
    }
  }
}
